test_unkpr_003 ← {
    a ← 2 1 ⍴ (1÷(2*0.5)) (1÷(2*0.5))
    correct_result ← 2 1 ⍴ (a) (a)
    result ← #.quapl.mlt.unkpr 4 1 ⍴ 0.5 0.5 0.5 0.5

    'Testing the decomposition of an equal superposition in every state'⊢ (⊃correct_result) Assert (⊃result):
    ''

}