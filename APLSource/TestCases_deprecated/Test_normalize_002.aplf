 R←Test_normalize_002(stopFlag batchFlag);⎕TRAP;md;ns;parms
⍝ Tests normalization of equal vector


 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 correct_vector←(1÷17*0.5)(4÷17*0.5)
 result←#.quapl.mlt.normalize 2 8

 R←T.PassesIf result≡correct_vector

 R ← T._OK
